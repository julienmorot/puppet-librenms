# Change RANDOMSTRINGGOESHERE to your preferred SNMP community string
com2sec readonly  default         <%= scope.lookupvar("librenms::vars::snmpcommunity") %>

group MyROGroup v2c        readonly
view all    included  .1                               80
access MyROGroup ""      any       noauth    exact  all    none   none

syslocation "<%= scope.lookupvar("librenms::vars::snmplocation") %>"
syscontact "<%= scope.lookupvar("librenms::vars::snmpcontact") %>" 

